# TODO

* How to use this repository?
* The `requirements.txt` used does not lock exact versions and therefore reproduction is not guaranteed.
* Consolidate the manually specified npm packages that are installed in the `Dockerfile` in `package.json` (and lock it), and install (globally) from there.
